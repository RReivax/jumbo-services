{
  "configurations": [
    {
      "core-site": {
        "ha.zookeeper.quorum": "{{ ZOOKEEPER.quorum }}
      }
    }
    {% if HIVE is defined %}
    ,{
      "hive-site": {
        "hive.zookeeper.quorum": "{{ ZOOKEEPER.quorum }}
      }
    },
    {
      "webhcat-site": {
        "templeton.zookeeper.hosts": "{{ ZOOKEEPER.quorum }}"
      }
    }
    {% endif %}
    {% if HBASE is defined %}
    ,{
      "hbase-site": {
        "hbase.zookeeper.hosts" : "{{ ZOOKEEPER.quorum_no_port }}"
      }
    }
    {% endif %}
  ]
}
